import React from 'react';

const LacZCards = ({
    dilutionFactor,
    volume,
    ownProtocolId,
    ownStrainId,
    ownStrainName,
    collectionData,
}) => {
    return (
        <div>
            {ownStrainName}
        </div>
    )
}

export default LacZCards;
