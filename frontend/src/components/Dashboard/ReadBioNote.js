import React from 'react'

const ReadBioNote = ({ id }) => {
    console.log(id);
    
    return (
        <div>
            Place for reading bionotes
        </div>
    )
}

export default ReadBioNote
